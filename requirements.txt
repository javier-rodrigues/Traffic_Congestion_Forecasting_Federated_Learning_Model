# --- Federated Traffic Forecasting (paper + assets) ---
# Pinned for reproducible EDA, training, FL simulation, and figure export.
numpy>=1.24,<2.1
pandas>=2.0,<2.3
matplotlib>=3.8,<3.9
seaborn>=0.13,<0.14

# Plotly + static PNG export (kaleido version compatible with Plotly 5.x)
plotly==5.24.1
kaleido==0.2.1
packaging>=23.2

# Classical ML baselines
scikit-learn>=1.3,<1.5

# Deep learning stacks (both are used: RNN and GNN variants)
tensorflow>=2.12,<2.16
tensorboard>=2.12,<2.16

torch>=2.2
torchvision>=0.17

# Federated learning (jobs, client API, simulator)
nvflare>=2.5.0
